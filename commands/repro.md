---
description: Drive the app until the bug or the feature is on screen, capture it, and render the evidence to a local folder
argument-hint: "[the bug to reproduce, or the feature to demonstrate]"
---

Invoke the `repro` skill by name and work the subject below.

- Name the skill (`repro`); never load it by path — the path differs per machine and the name does not.
- Nothing has been captured yet. The app has to be driven before there is anything to show.
- The run ends in a local folder. Posting to GitHub is a separate, explicit ask.

Subject: $ARGUMENTS
